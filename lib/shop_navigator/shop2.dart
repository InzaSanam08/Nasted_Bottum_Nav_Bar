import 'package:flutter/material.dart';

class Shop_Screen2 extends StatelessWidget {
  const Shop_Screen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Shop Screen 2")));
  }
}
